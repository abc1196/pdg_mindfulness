<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mindfulness.pdg_mindfulness">

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.flash" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Alt">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dashboard.HomeActivity" />
        <activity android:name=".measurement.PSTActivity" />
        <activity android:name=".measurement.HRVActivity" />
        <activity android:name=".dashboard.WelcomeActivity" />
        <activity
            android:name=".graphics.GraphicsActivity"
            android:label="@string/title_activity_graphics"
            android:theme="@style/AppTheme.Alt" />
        <activity android:name=".treatment.TreatmentActivity"/>
    </application>

</manifest>